# Core-APIs
Core solvers for APIs with predefined inputs and outputs that are REST ready. Each folder has a small solution of its own.

AI Instructions
All AI Skills and agents are located under "ai-skills" folder, a submodule repository
Temporary work area for AI is "AI-Playground"
Each API will have the same repeating structure, with a minimum "Spec" folder and a ReadMe.md file at the root
You will maintain a register of each prompts and your reponse text at the root of the module folder as a markdown file
 
